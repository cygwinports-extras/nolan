DESCRIPTION="Nolan-chart political quiz"
HOMEPAGE="http://www.catb.org/~esr/${PN}/"
SRC_URI="http://www.catb.org/~esr/${PN}/${P}.tar.gz"

src_compile() {
	lndirs
	cd ${B}
	PATH="${B}:${PATH}"
	cygmake CFLAGS="${CFLAGS} -DSIGIOT=SIGABRT \$(SYSTYPE)"
}

src_install() {
	cd ${B}
	dobin ${PN}.exe
	doman ${PN}.1
}
